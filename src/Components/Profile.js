import React from 'react';
import { useParams } from 'react-router-dom';

const Profile = (props) => {

    // const { id } = useParams();

    // const foundProfile = props.beers.find(user => {
    //     return user.name === id;
    // })

    return(

         <h2>Beer Profile Page</h2>


    )
}
export default Profile;